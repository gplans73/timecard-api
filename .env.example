# Environment Variables for Go Timecard API
# Copy this file to .env for local development
# Set these in your hosting dashboard for production

# SMTP Configuration
# These are required for the /api/email-timecard endpoint

# SMTP Host (mail server address)
SMTP_HOST=smtp.gmail.com

# SMTP Port (usually 587 for TLS, 465 for SSL)
SMTP_PORT=587

# SMTP Username (usually your email address)
SMTP_USERNAME=your-email@gmail.com

# SMTP Password
# For Gmail: Use App Password (requires 2FA enabled)
# Generate at: https://myaccount.google.com/apppasswords
SMTP_PASSWORD=your-app-password

# From Address (optional, defaults to SMTP_USERNAME)
SMTP_FROM=your-email@gmail.com

# ─────────────────────────────────────────────────────
# SMTP Provider Examples
# ─────────────────────────────────────────────────────

# Gmail
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-16-char-app-password

# SendGrid
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USERNAME=apikey
# SMTP_PASSWORD=your-sendgrid-api-key

# AWS SES
# SMTP_HOST=email-smtp.us-east-1.amazonaws.com
# SMTP_PORT=587
# SMTP_USERNAME=your-ses-username
# SMTP_PASSWORD=your-ses-password

# Microsoft 365
# SMTP_HOST=smtp.office365.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@company.com
# SMTP_PASSWORD=your-password

# Outlook.com
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@outlook.com
# SMTP_PASSWORD=your-password

# Yahoo Mail
# SMTP_HOST=smtp.mail.yahoo.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@yahoo.com
# SMTP_PASSWORD=your-app-password

# ─────────────────────────────────────────────────────
# Optional: API Security (Recommended for Production)
# ─────────────────────────────────────────────────────

# API Key for authentication (not currently implemented)
# API_KEY=your-secret-api-key

# Port to run server on (defaults to 8080)
# PORT=8080

# Environment (development/production)
# ENV=production
